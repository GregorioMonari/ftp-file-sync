{
  "name": "client-synchronizer",
  "version": "0.0.1",
  "description": "Sync filesystem with an ftp server",
  "main": "./build/main.js",
  "scripts": {
    "start": "node ./build/main",
    "build": "tsc",
    "dev:build": "tsc -w",
    "dev:run": "nodemon build/main",
    "start:dev": "concurrently npm:dev:*",
    "start:build": "tsc && node ./build/main"
  },
  "author": "Gregorio Monari",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "@types/ws": "^8.5.11",
    "basic-ftp": "^5.0.5",
    "watcher": "^2.3.1",
    "winston": "^3.14.2",
    "ws": "^8.18.0"
  }
}
